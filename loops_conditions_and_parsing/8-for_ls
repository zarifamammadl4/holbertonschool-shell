#!/usr/bin/env bash
# Display filenames after first dash

for file in *; do
    # Skip if not a regular file
    [ -f "$file" ] || continue

    case "$file" in
        *-*)
            echo "${file#*-}"
            ;;
        *)
            echo "$file"
            ;;
    esac
done

